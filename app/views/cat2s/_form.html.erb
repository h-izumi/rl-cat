<%= form_with(model: cat2, local: true) do |form| %>
  <% if cat2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cat2.errors.count, "error") %> prohibited this cat2 from being saved:</h2>

      <ul>
      <% cat2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cat1_id %>
    <%= form.collection_radio_buttons :cat1_id, Cat1.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
